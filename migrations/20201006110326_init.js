// record all changes made to the database schema

//the up() function defines/holds/descripbes the changes to apploy to the db when the migration runs
exports.up = function(knex) {
  
};

//the down() function describes how to undo the changes
exports.down = function(knex) {
  
};
